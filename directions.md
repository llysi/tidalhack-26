install ai + featherless sdk:
npm install ai @ai-sdk/openai-compatible

run by doing:
npm run dev

open local webpage by pasting this in browser:
http://localhost:3000

close webpage by doing this in terminal:
^C

